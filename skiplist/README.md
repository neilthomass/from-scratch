A from-scratch C++ skip list, a probabilistic ordered structure giving O(log n) expected add/search/erase without the rebalancing of a tree.

Coin-flip node heights build express lanes over a sorted linked list, so searches skip ahead instead of walking every node. It's what powers Redis sorted sets and LevelDB's memtable.
